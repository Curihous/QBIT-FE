name: qbit_core
description: QBIT Core package - 공통 핵심 기능
version: 1.0.0

environment:
  sdk: ">=3.6.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_dotenv: ^6.0.0
  logger: ^2.0.2
  shared_preferences: ^2.5.3
  flutter_secure_storage: ^9.2.2
  json_annotation: ^4.9.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  json_serializable: ^6.8.0
  build_runner: ^2.7.1
