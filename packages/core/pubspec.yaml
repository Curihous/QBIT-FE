name: qbit_core
description: QBit Core - 핵심 설정 및 모델
version: 1.0.0
publish_to: none

environment:
  sdk: ">=3.6.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  
  # 환경 설정
  flutter_dotenv: ^6.0.0
  
  # 로깅
  logger: ^2.0.2
  
  # JSON
  json_annotation: ^4.9.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  json_serializable: ^6.8.0
  build_runner: ^2.7.1
